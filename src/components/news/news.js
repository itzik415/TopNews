import React from 'react';

const news = (props) => {
    // onClick={props.buttonClick}
    return (
        <div className="news__section">
             
             <button className="news__section">click for more</button>
        </div>
    )
}

export default news;